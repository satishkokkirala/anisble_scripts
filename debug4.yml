case4:debug msg displays the inventory hostname

---
 - name: debug msg inventory hostname
   hosts: all
   tasks:
      - name: displays inventory_hostname
	    debug:
		  msg: "the hostname is :{{inventory_hostname}}"
